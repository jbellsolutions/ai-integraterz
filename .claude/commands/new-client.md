Initialize a new client from a discovery call.

Usage: /new-client <client-id> [transcript file path]

Steps:
1. Run `./lib/new-client.sh $ARGUMENTS`
2. Report back what was created and any clarifications needed
3. Show the path to review: `config/clients/<client-id>.json`
